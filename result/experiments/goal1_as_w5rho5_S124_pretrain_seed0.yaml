# Goal 1 Ablation 1.2: Mode sep rho=5 — AS on W5
goal: 1
stage: pretrain
seed: 0
n_epochs: 500

model:
  config: model/configs/as_default.yaml

benchmark:
  config: benchmark/configs/w5_rho5.yaml

subset:
  S: [1, 2, 4]
  full_target: false

init:
  from_checkpoint: null
  noise_level: 0.0

logging:
  checkpoint_every: 500
  eval_every: 0
  n_eval_samples: 10000

output:
  checkpoint_dir: result/checkpoints/goal1_as_w5rho5_S124_pretrain_seed0/
  log_file: result/logs/goal1_as_w5rho5_S124_pretrain_seed0.csv

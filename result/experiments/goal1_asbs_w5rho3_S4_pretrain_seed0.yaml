# Goal 1 Ablation 1.2: Mode sep rho=3 — ASBS on W5
goal: 1
stage: pretrain
seed: 0
n_epochs: 500

model:
  config: model/configs/asbs_default.yaml

benchmark:
  config: benchmark/configs/w5_rho3.yaml

subset:
  S: [4]
  full_target: false

init:
  from_checkpoint: null
  noise_level: 0.0

logging:
  checkpoint_every: 500
  eval_every: 0
  n_eval_samples: 10000

output:
  checkpoint_dir: result/checkpoints/goal1_asbs_w5rho3_S4_pretrain_seed0/
  log_file: result/logs/goal1_asbs_w5rho3_S4_pretrain_seed0.csv

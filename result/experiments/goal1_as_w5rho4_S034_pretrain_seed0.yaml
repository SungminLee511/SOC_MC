# Goal 1 Ablation 1.2: Mode sep rho=4 — AS on W5
goal: 1
stage: pretrain
seed: 0
n_epochs: 500

model:
  config: model/configs/as_default.yaml

benchmark:
  config: benchmark/configs/w5_rho4.yaml

subset:
  S: [0, 3, 4]
  full_target: false

init:
  from_checkpoint: null
  noise_level: 0.0

logging:
  checkpoint_every: 500
  eval_every: 0
  n_eval_samples: 10000

output:
  checkpoint_dir: result/checkpoints/goal1_as_w5rho4_S034_pretrain_seed0/
  log_file: result/logs/goal1_as_w5rho4_S034_pretrain_seed0.csv
